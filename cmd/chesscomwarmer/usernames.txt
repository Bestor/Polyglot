# One chess.com username per line. Blank lines and lines starting with #
# are ignored. Edit this file directly - if running via docker compose,
# it's bind-mounted into the chesscomwarmer container so changes take
# effect on the next hourly pass, no rebuild/restart needed. Empty by
# default - a safe no-op until usernames are added.
bestor0
